{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build:bundle": "vite build",
    "build:wasm": "npx jco componentize dist/component.js --wit wit --world-name handler -o dist/handler.wasm",
    "build": "npm run build:bundle && npm run build:wasm",
    "clean": "rm -rf dist *.wasm",
    "test": "./scripts/test.sh"
  },
  "keywords": ["wasi", "wasm", "typescript", "mik"],
  "author": "{{AUTHOR_NAME}}",
  "license": "MIT",
  "description": "mik handler in TypeScript",
  "devDependencies": {
    "@bytecodealliance/componentize-js": "^0.19.3",
    "@bytecodealliance/jco": "^1.15.4",
    "typescript": "^5.9.3",
    "vite": "^6.0.0"
  }
}
